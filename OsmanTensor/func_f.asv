function ret_val = func_f(A, x)
%FUNC_F     Returns function f.
%   This is used with FASTA package.

AA = cell(3,1);
no_row = size(A,1)/3;
for k = 1:3
    AA{k} = A(1 + (k-1)*no_row : k*no_row, :);
end

ret_val = 1/2 * norm(tensor(X) - tensor(ktensor(AA)))^2;

end

